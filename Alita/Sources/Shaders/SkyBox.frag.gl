

//layout (binding = 1) uniform samplerCube samplerCubeMap;

layout (location = 0) in vec3 inUVW;

layout(binding = 11) uniform textureCube    tAlbedo;
layout(binding = 12) uniform sampler        sAlbedo;


layout(location = 0) out vec4 OutColor;

void TSkyBox() 
{
    OutColor = texture(samplerCube(tAlbedo, sAlbedo), inUVW);
	//OutColor = texture(tAlbedo, inUVW);
    // OutColor = vec4(inUVW, 1.0);
}

